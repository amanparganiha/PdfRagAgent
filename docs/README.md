# docs

Put your app screenshot here as `screenshot.png` — the main README references it.

Quick way: deploy the app, open it, take a screenshot of the chat interface
with a PDF loaded, and save it as `docs/screenshot.png`.
