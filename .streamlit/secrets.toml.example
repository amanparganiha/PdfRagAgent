# Rename this file to secrets.toml for LOCAL testing (it's git-ignored).
# For Streamlit Cloud, paste these into the "Secrets" box in your app settings instead.

OPENAI_API_KEY = "sk-proj-your-actual-key-here"
MODEL = "gpt-4o"
